version: "3.9"

name: fizzup

services:
  fizzup_postgres:
    image: postgres:${POSTGRES_VERSION}
    container_name: ${COMPOSE_PROJECT_NAME}_postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ${POSTGRES_VOLUME}:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT}:5432"

  fizzup_adminer:
    image: adminer:${ADMINER_VERSION}
    container_name: ${COMPOSE_PROJECT_NAME}_adminer
    restart: always
    depends_on:
      - fizzup_postgres
    ports:
      - "${ADMINER_PORT}:8080"

  fizzup_redis:
    image: redis:${REDIS_VERSION}
    container_name: ${COMPOSE_PROJECT_NAME}_redis
    command: >
      sh -c '
        if [ -n "$REDIS_PASSWORD" ]; then
          echo "requirepass ${REDIS_PASSWORD}" > /usr/local/etc/redis/requirepass.conf;
          cat /usr/local/etc/redis/requirepass.conf /usr/local/etc/redis/redis.conf > /usr/local/etc/redis/redis-final.conf;
          exec redis-server /usr/local/etc/redis/redis-final.conf;
        else
          exec redis-server /usr/local/etc/redis/redis.conf;
        fi
      '
    environment:
      TZ: ${TZ}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - ./redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ${REDIS_VOLUME}:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$REDIS_PASSWORD\" ping | grep -q PONG || ( [ -z \"$REDIS_PASSWORD\" ] && redis-cli ping | grep -q PONG )"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  fizzup_redis_commander:
    image: rediscommander/redis-commander:${REDIS_COMMANDER_VERSION}
    container_name: ${COMPOSE_PROJECT_NAME}_redis_commander
    environment:
      REDIS_HOST: fizzup_redis
      REDIS_PORT: 6379
      REDIS_DB: 0
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      HTTP_USER: ${REDIS_COMMANDER_USER}
      HTTP_PASSWORD: ${REDIS_COMMANDER_PASSWORD}
      TZ: ${TZ}
    depends_on:
      - fizzup_redis
    ports:
      - "${REDIS_COMMANDER_PORT}:8081"
    restart: unless-stopped

networks:
  default:
    name: ${COMPOSE_PROJECT_NAME}_network

volumes:
  fizzup_postgres_data:
  fizzup_redis_data: